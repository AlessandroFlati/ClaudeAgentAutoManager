name: test.crash
version: 1
change_type: net_new
description: Exits with a non-standard code.
inputs: {}
outputs:
  never:
    schema: Integer
implementation:
  language: python
  entry_point: tool.py:run
