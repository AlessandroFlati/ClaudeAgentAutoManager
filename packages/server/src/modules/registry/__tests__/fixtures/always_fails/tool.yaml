name: test.always_fails
version: 1
change_type: net_new
description: Always raises.
inputs:
  message:
    schema: String
    required: true
outputs:
  never:
    schema: String
implementation:
  language: python
  entry_point: tool.py:run
